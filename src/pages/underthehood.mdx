import Layout from "../components/Layout"

# Under the hood

Fachenteo is a static website built with gatsbyJS as a perssonal site for photography, its intended purpose is to serve both as a repository of images
and a learning experience for me. As stated above the main technology driven the site is GatsbyJS and run inside a simple EC2 AWS instance. Here it's describe 
the gatsby structure and deployment practice for the site.



export default Layout